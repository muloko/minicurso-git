git config --global user.nome = "meu nome"
git config --global user.email = "meu email"
$ git config --global color.ui auto
git init
git status
git add
git commit
git log
git checkout -b getname 		(Ja cria a branch no caso se chama getname)

git more add origin https://github.com/TrekRoX/minicurso-git.git